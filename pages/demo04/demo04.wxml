<!--pages/demo04/demo04.wxml-->
<!-- 1 需要给input标签绑定 input 事件 
  绑定关键字 bindinput -->
  <!-- 赋值： this.setData({
    num: e.detail.value
  })
  点击事件 
  1 bindtap
  2 无法在小程序当中的事件中 直接传参
  3  通过自定义属性的方式传参
  4 事件源中获取自定义属性-->
<input type="text" bindinput="handleInput"/>
<button bindtap="hanleTap" data-operation="{{1}}">+</button>
<button bindtap="hanleTap" data-operation="{{-1}}">-</button>
<view>
  {{num}}
</view>
